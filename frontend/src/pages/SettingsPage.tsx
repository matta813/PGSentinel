import{FormEvent,useState}from'react';import{Send}from'lucide-react';import{api,APIError}from'../api/client';export function SettingsPage(){const[provider,setProvider]=useState('ntfy');const[form,setForm]=useState({serverUrl:'https://ntfy.sh',topic:'',token:'',username:'',password:'',webhookUrl:''});const[status,setStatus]=useState('');async function submit(e:FormEvent){e.preventDefault();setStatus('Sending…');try{await api.post('/notifications/test',{provider,...form});setStatus('Test notification delivered successfully.')}catch(e){setStatus(e instanceof APIError?`${e.message}: ${e.detail}`:'Notification failed')}}return <><div className="title-row"><div><p className="eyebrow">Settings</p><h1>Notifications</h1><p>Verify delivery before depending on an alert destination.</p></div></div><form className="settings-card" onSubmit={submit}><label>Provider<select value={provider} onChange={e=>setProvider(e.target.value)}><option value="ntfy">ntfy</option><option value="webhook">Generic webhook</option></select></label>{provider==='ntfy'?<><label>Server URL<input type="url" required value={form.serverUrl} onChange={e=>setForm({...form,serverUrl:e.target.value})}/></label><label>Topic<input required value={form.topic} onChange={e=>setForm({...form,topic:e.target.value})}/></label><label>Token (optional)<input type="password" value={form.token} onChange={e=>setForm({...form,token:e.target.value})}/></label><label>Username (optional)<input value={form.username} onChange={e=>setForm({...form,username:e.target.value})}/></label><label>Password (optional)<input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label></>:<label>Webhook URL<input type="url" required value={form.webhookUrl} onChange={e=>setForm({...form,webhookUrl:e.target.value})}/></label>}<button type="submit"><Send/> Send test notification</button>{status&&<div className="notice">{status}</div>}</form></>}
