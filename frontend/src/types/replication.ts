export interface ReplicationStandby { Application:string; ClientAddress:string; State:string; SyncState:string; ReplayLagSeconds:number; PendingSendBytes:number; PendingWriteBytes:number; PendingFlushBytes:number; PendingReplayBytes:number; PendingReplayGrowthBytesPerSecond:number; ReplyAgeSeconds:number }
export interface ReplicationSlot { Name:string; Type:string; Database:string; WALStatus:string; Active:boolean; RetainedBytes:number; InactiveSeconds:number; RetainedGrowthBytesPerSecond:number }
export interface ReplicationStats { InRecovery:boolean; TimelineID:number; RecoveryPaused:boolean; ReplayDelaySeconds:number; ReplayLSN:string; ReceiveLSN:string; Standbys:ReplicationStandby[]; Slots:ReplicationSlot[]; Receiver?:{Status:string;SenderHost:string;LatestEndLSN:string;LastMessageSeconds:number} }
export interface WALStats { ArchiveMode:string; ArchiveConfigured:boolean; ArchivedCount:number; FailedArchiveCount:number; LastArchivedWAL:string; LastFailedWAL:string; LastArchivedAt?:string; LastFailedAt?:string; WALBytes:number; GenerationBytesPerSecond:number; WALBuffersFull:number; BufferFullEventsPerSecond:number; TimedCheckpoints:number; RequestedCheckpoints:number; RestartpointsTimed:number; RestartpointsRequested:number; RestartpointsDone:number; CurrentLSN:string }
