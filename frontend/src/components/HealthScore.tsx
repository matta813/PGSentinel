export function HealthScore({value}:{value:number}){const label=value>=90?'Healthy':value>=70?'Needs attention':value>=40?'Degraded':'Critical';return <article className="score-card" aria-label={`Health score ${value} of 100, ${label}`}><span>Overall health · {label}</span><strong>{value}</strong><small>/ 100</small><div className="bar"><i style={{width:`${Math.max(0,Math.min(100,value))}%`}}/></div></article>}
