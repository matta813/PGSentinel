import{render,screen}from'@testing-library/react';import{describe,expect,it}from'vitest';import{HealthScore}from'./HealthScore';describe('HealthScore',()=>{it('explains a degraded score accessibly',()=>{render(<HealthScore value={62}/>);expect(screen.getByLabelText(/62 of 100, Degraded/)).toBeInTheDocument()});it('clamps visual percentages',()=>{const{container}=render(<HealthScore value={120}/>);expect(container.querySelector('.health-track i')).toHaveStyle({width:'100%'})})})
