import type{Severity}from'../types';export function SeverityBadge({severity}:{severity:Severity}){return <span className={`badge ${severity.toLowerCase()}`}>{severity}</span>}export function Empty({title,detail}:{title:string;detail:string}){return <div className="empty"><div className="empty-icon">✓</div><h3>{title}</h3><p>{detail}</p></div>}export function Loading(){return <div className="skeletons"><i/><i/><i/></div>}export function ErrorState({error,retry}:{error:Error;retry?:()=>void}){return <div className="error"><strong>Unable to load data</strong><p>{error.message}</p>{retry&&<button onClick={retry}>Try again</button>}</div>}
